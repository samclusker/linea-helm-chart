apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ include "linea-dev.name" . }}-storage-class
provisioner: ebs.csi.aws.com
parameters:
  type: gp3
  encrypted: "true"
volumeBindingMode: WaitForFirstConsumer